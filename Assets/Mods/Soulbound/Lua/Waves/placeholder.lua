-- Placeholder wave for the Soulbound mod.
-- It spawns no bullets, so the defending round simply runs out the encounter's
-- wavetimer. Replace it once real content lands.

function Update()
    -- Runs every frame while the wave is active.
end

function OnHit(bullet)
    -- Runs when a bullet hits the player.
end
